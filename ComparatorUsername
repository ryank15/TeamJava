package teamjava.courses.project;

import java.util.Comparator;

public class ComparatorUsername implements Comparator<Favorites> {
	@Override
	public int compare(Favorites arg0, Favorites arg1) {
		return arg0.getUserName().compareTo(arg1.getUserName());
	}
}
